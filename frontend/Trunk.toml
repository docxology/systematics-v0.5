[[hooks]]
stage = "build"
command = "echo"
command_arguments = ["Building systematics-interface..."]

[build]
target = "index.html"
# Disable wasm-opt due to parsing errors with Rust 1.93.0 output
wasm-opt = false

[watch]
# ignore = ["target"]

[serve]
address = "127.0.0.1"
port = 8080
